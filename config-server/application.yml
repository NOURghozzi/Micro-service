server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/NOURghozzi/ton-repo-config
          clone-on-start: true
  application:
    name: config-server
